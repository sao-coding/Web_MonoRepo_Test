# =============================================================================
# aiforce - 環境變數模板
# =============================================================================
# 使用方式: 複製此檔案為 .env 並填入實際值

# =============================================================================
# 網站基礎設定
# =============================================================================
NEXT_PUBLIC_BASE_PATH_URL=/AI_City

# =============================================================================
# API 服務 (dev/prod 值不同)
# =============================================================================
# 首頁網址
NEXT_PUBLIC_RD_SITE_URL={$RD_SITE_URL}

# 登入服務 API URL
NEXT_PUBLIC_LOGIN_API_URL={$LOGIN_API_URL}

# 登入 cookie 網域設定
NEXT_PUBLIC_COOKIE_DOMAIN={$COOKIE_DOMAIN}

# 專利服務 API URL (patents, pcb 功能使用)
NEXT_PUBLIC_PATENT_SERVICE_API_URL={$PATENT_SERVICE_API_URL}

# NIM 服務 API URL (asr, tts, translate, img2text, instant 功能使用)
NEXT_PUBLIC_NIM_SERVICE_API_URL={$NIM_SERVICE_API_URL}

# AI 端 API
NEXT_PUBLIC_AI_API_URL={$AI_API_URL}

# VGA AI API
NEXT_PUBLIC_VGA_AI_API_URL={$VGA_AI_API_URL}

# AIforce API
NEXT_PUBLIC_AIforce_API_URL={$AIforce_API_URL}

# =============================================================================
# 第三方服務 (放 .config)
# =============================================================================
# NEXT_PUBLIC_POSTHOG_KEY={$POSTHOG_KEY}
# NEXT_PUBLIC_POSTHOG_HOST={$POSTHOG_HOST}

# =============================================================================
# 機密參數 (放 .config)
# =============================================================================
# DATABASE_URL={$DATABASE_URL}
# JWT_SECRET_KEY={$JWT_SECRET_KEY}
