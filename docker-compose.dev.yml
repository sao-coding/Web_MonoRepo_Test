# Docker Compose for Development Environment (10.16.20.11)
# 使用方式: docker compose -f docker-compose.dev.yml up -d --build
#
# Port 配置說明:
# - Traefik: 80 (HTTP), 8080 (Dashboard)
# - 各 App 內部 Port 不對外暴露，由 Traefik 統一路由
# - 避免與現有 PM2 服務衝突 (3001-3003)

# 自動載入環境變數檔案（不需要 --env-file 參數）
x-env-file: &env-file .env.dev

services:
  traefik:
    image: traefik:v3.6.6
    container_name: traefik-dev
    command:
      - --api.dashboard=true
      - --api.insecure=true
      - --providers.docker=true
      - --providers.docker.exposedbydefault=false
      - --entrypoints.web.address=:80
      - --log.level=DEBUG
    ports:
      - "3000:80"
      - "8090:8080"  # Dashboard
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
    networks:
      - ai-city
    restart: unless-stopped

  rd-aicity:
    build:
      context: .
      dockerfile: apps/rd_aicity/Dockerfile
      args:
        APP_ENV: development
        # NEXT_PUBLIC 變數需要在 build time 傳入
        NEXT_PUBLIC_RD_SITE_URL: ${NEXT_PUBLIC_RD_SITE_URL}
        NEXT_PUBLIC_LOGIN_API_URL: ${NEXT_PUBLIC_LOGIN_API_URL}
        NEXT_PUBLIC_BASE_PATH_URL: ${NEXT_PUBLIC_BASE_PATH_URL:-/AI_City}
        NEXT_PUBLIC_COOKIE_DOMAIN: ${NEXT_PUBLIC_COOKIE_DOMAIN}
        NEXT_PUBLIC_NIM_SERVICE_API_URL: ${NEXT_PUBLIC_NIM_SERVICE_API_URL}
        NEXT_PUBLIC_PATENT_SERVICE_API_URL: ${NEXT_PUBLIC_PATENT_SERVICE_API_URL}
        NEXT_PUBLIC_AI_API_URL: ${NEXT_PUBLIC_AI_API_URL}
        NEXT_PUBLIC_VGA_AI_API_URL: ${NEXT_PUBLIC_VGA_AI_API_URL}
        NEXT_PUBLIC_AIforce_API_URL: ${NEXT_PUBLIC_AIforce_API_URL:-}
        NEXT_PUBLIC_POSTHOG_KEY: ${NEXT_PUBLIC_POSTHOG_KEY:-}
        NEXT_PUBLIC_POSTHOG_HOST: ${NEXT_PUBLIC_POSTHOG_HOST:-}
    container_name: rd-aicity-dev
    environment:
      - NODE_ENV=production
      - APP_ENV=development
      - PORT=3001
      - HOSTNAME=0.0.0.0
    env_file:
      - .env       # 公開參數 (URLs/IPs) - 上版控
      - .config    # 機密參數 (tokens/keys) - CI/CD 注入
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.aicity.rule=PathPrefix(`/AI_City`)"
      - "traefik.http.routers.aicity.priority=1"
      - "traefik.http.routers.aicity.entrypoints=web"
      - "traefik.http.services.aicity.loadbalancer.server.port=3001"
    networks:
      - ai-city
    restart: unless-stopped
    depends_on:
      - traefik

  product-spec:
    build:
      context: .
      dockerfile: apps/product-spec/Dockerfile
      args:
        APP_ENV: development
    container_name: product-spec-dev
    environment:
      - NODE_ENV=production
      - APP_ENV=development
      - PORT=3002
      - HOSTNAME=0.0.0.0
    env_file:
      - .env       # 公開參數 (URLs/IPs) - 上版控
      - .config    # 機密參數 (tokens/keys) - CI/CD 注入
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.productspec.rule=PathPrefix(`/AI_City/ProductSpec`)"
      - "traefik.http.routers.productspec.priority=10"
      - "traefik.http.routers.productspec.entrypoints=web"
      - "traefik.http.services.productspec.loadbalancer.server.port=3002"
    networks:
      - ai-city
    restart: unless-stopped
    depends_on:
      - traefik

  asr:
    build:
      context: .
      dockerfile: apps/asr/Dockerfile
      args:
        APP_ENV: development
    container_name: asr-dev
    environment:
      - NODE_ENV=production
      - APP_ENV=development
      - PORT=3003
      - HOSTNAME=0.0.0.0
    env_file:
      - .env       # 公開參數 (URLs/IPs) - 上版控
      - .config    # 機密參數 (tokens/keys) - CI/CD 注入
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.asr.rule=PathPrefix(`/AI_City/asr`)"
      - "traefik.http.routers.asr.priority=10"
      - "traefik.http.routers.asr.entrypoints=web"
      - "traefik.http.services.asr.loadbalancer.server.port=3003"
    networks:
      - ai-city
    restart: unless-stopped
    depends_on:
      - traefik

  translate:
    build:
      context: .
      dockerfile: apps/translate/Dockerfile
      args:
        APP_ENV: development
    container_name: translate-dev
    environment:
      - NODE_ENV=production
      - APP_ENV=development
      - PORT=3004
      - HOSTNAME=0.0.0.0
    env_file:
      - .env
      - .config
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.translate.rule=PathPrefix(`/AI_City/translate`)"
      - "traefik.http.routers.translate.priority=10"
      - "traefik.http.routers.translate.entrypoints=web"
      - "traefik.http.services.translate.loadbalancer.server.port=3004"
    networks:
      - ai-city
    restart: unless-stopped
    depends_on:
      - traefik

  tts:
    build:
      context: .
      dockerfile: apps/tts/Dockerfile
      args:
        APP_ENV: development
    container_name: tts-dev
    environment:
      - NODE_ENV=production
      - APP_ENV=development
      - PORT=3005
      - HOSTNAME=0.0.0.0
    env_file:
      - .env
      - .config
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.tts.rule=PathPrefix(`/AI_City/tts`)"
      - "traefik.http.routers.tts.priority=10"
      - "traefik.http.routers.tts.entrypoints=web"
      - "traefik.http.services.tts.loadbalancer.server.port=3005"
    networks:
      - ai-city
    restart: unless-stopped
    depends_on:
      - traefik

  img2text:
    build:
      context: .
      dockerfile: apps/img2text/Dockerfile
      args:
        APP_ENV: development
    container_name: img2text-dev
    environment:
      - NODE_ENV=production
      - APP_ENV=development
      - PORT=3006
      - HOSTNAME=0.0.0.0
    env_file:
      - .env
      - .config
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.img2text.rule=PathPrefix(`/AI_City/img2text`)"
      - "traefik.http.routers.img2text.priority=10"
      - "traefik.http.routers.img2text.entrypoints=web"
      - "traefik.http.services.img2text.loadbalancer.server.port=3006"
    networks:
      - ai-city
    restart: unless-stopped
    depends_on:
      - traefik

  patents:
    build:
      context: .
      dockerfile: apps/patents/Dockerfile
      args:
        APP_ENV: development
    container_name: patents-dev
    environment:
      - NODE_ENV=production
      - APP_ENV=development
      - PORT=3007
      - HOSTNAME=0.0.0.0
    env_file:
      - .env
      - .config
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.patents.rule=PathPrefix(`/AI_City/patents`)"
      - "traefik.http.routers.patents.priority=10"
      - "traefik.http.routers.patents.entrypoints=web"
      - "traefik.http.services.patents.loadbalancer.server.port=3007"
    networks:
      - ai-city
    restart: unless-stopped
    depends_on:
      - traefik

  pcb:
    build:
      context: .
      dockerfile: apps/pcb/Dockerfile
      args:
        APP_ENV: development
    container_name: pcb-dev
    environment:
      - NODE_ENV=production
      - APP_ENV=development
      - PORT=3008
      - HOSTNAME=0.0.0.0
    env_file:
      - .env
      - .config
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.pcb.rule=PathPrefix(`/AI_City/pcb`)"
      - "traefik.http.routers.pcb.priority=10"
      - "traefik.http.routers.pcb.entrypoints=web"
      - "traefik.http.services.pcb.loadbalancer.server.port=3008"
    networks:
      - ai-city
    restart: unless-stopped
    depends_on:
      - traefik

networks:
  ai-city:
    driver: bridge
